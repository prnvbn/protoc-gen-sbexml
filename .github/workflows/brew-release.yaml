name: Update Homebrew Formula

on:
  release:
    types: [edited]

  workflow_dispatch:
    inputs:
      version:
        description: "Version to update the formula to (optional)"
        required: false
        default: ""

permissions:
  contents: write

jobs:
  check-latest:
    if: ${{ github.event_name == 'release' }}
    runs-on: ubuntu-latest
    outputs:
      is-latest: ${{ steps.release.outputs.is-latest }}

    steps:
      - name: Check edited release is latest
        id: release
        uses: actions/github-script@v7
        with:
          script: |
            let latest;
            try {
              latest = await github.rest.repos.getLatestRelease({
                owner: context.repo.owner,
                repo: context.repo.repo,
              });
            } catch (error) {
              if (error.status === 404) {
                core.info("Repository does not have a latest full release yet.");
                core.setOutput("is-latest", "false");
                return;
              }
              throw error;
            }
            const isLatest = latest.data.id === context.payload.release.id;
            core.info(`Edited release ${context.payload.release.tag_name}; latest release is ${latest.data.tag_name}`);
            core.setOutput("is-latest", String(isLatest));

  update-formula:
    needs: [check-latest]
    if: ${{ always() && (github.event_name == 'workflow_dispatch' || needs.check-latest.outputs.is-latest == 'true') }}
    runs-on: ubuntu-latest

    steps:
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.GH_APP_ID }}
          private-key: ${{ secrets.GH_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: |
            homebrew-tap

      - name: Checkout homebrew-tap
        uses: actions/checkout@v4
        with:
          repository: prnvbn/homebrew-tap
          token: ${{ steps.app-token.outputs.token }}
          ref: main
          path: homebrew-tap

      - name: Set up Git
        run: |
          git config --global user.name "prnvbn-homebrew-tap-updater[bot]"
          git config --global user.email "prnvbn-homebrew-tap-updater@users.noreply.github.com"

      - name: Update Homebrew formula
        run: |
          set -e

          cd homebrew-tap

          FORMULA="Formula/protoc-gen-sbexml.rb"

          if [ -n "${{ github.event.inputs.version }}" ]; then
            TAG_VERSION="${{ github.event.inputs.version }}"
          elif [ "${{ github.event_name }}" = "release" ]; then
            TAG_VERSION="${{ github.ref_name }}"
          else
            TAG_VERSION="${GITHUB_REF_NAME#refs/tags/}"
          fi

          VERSION=${TAG_VERSION#v}

          if [[ ! "$TAG_VERSION" =~ ^v ]]; then
            TAG_VERSION="v${TAG_VERSION}"
          fi

          echo "Tag version: $TAG_VERSION"
          echo "Formula version: $VERSION"

          sed -i -E "s/^  version \".*\"/  version \"${VERSION}\"/" "$FORMULA"

          declare -A BINARIES=(
            ["protoc-gen-sbexml-darwin-arm64"]="darwin-arm64"
            ["protoc-gen-sbexml-darwin-amd64"]="darwin-amd64"
            ["protoc-gen-sbexml-linux-arm64"]="linux-arm64"
            ["protoc-gen-sbexml-linux-amd64"]="linux-amd64"
          )

          for file in "${!BINARIES[@]}"; do
            url="https://github.com/prnvbn/protoc-gen-sbexml/releases/download/${TAG_VERSION}/${file}"
            echo "Downloading $url"
            curl -L -f -o "/tmp/$file" "$url"

            sha256=$(shasum -a 256 "/tmp/$file" | awk '{print $1}')
            echo "SHA256 for $file: $sha256"

            sed -i -E "/${file}/{n;s/sha256 \".*\"/sha256 \"${sha256}\"/}" "$FORMULA"
          done

          if ! git diff --quiet; then
            git add "$FORMULA"
            git commit -m "Update protoc-gen-sbexml formula to ${VERSION}"

            APP_TOKEN="${{ steps.app-token.outputs.token }}"
            git remote set-url origin "https://x-access-token:${APP_TOKEN}@github.com/prnvbn/homebrew-tap.git"
            git push origin main
          else
            echo "No changes to commit"
          fi
