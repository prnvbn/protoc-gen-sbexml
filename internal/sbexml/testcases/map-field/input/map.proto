syntax = "proto3";
package maps;

enum Status {
  STATUS_UNSPECIFIED = 0;
  STATUS_ACTIVE = 1;
}

message Address {
  string city = 1;
}

message Directory {
  map<string, int32> scores = 1;
  map<uint32, Address> addresses = 2;
  map<string, Status> statuses = 3;
}
